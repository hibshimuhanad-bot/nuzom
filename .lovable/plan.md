

# خطة: تفعيل 4 أنظمة كـ "متوفر الآن"

## الوضع الحالي
حالياً النظام يعرض فقط أول منتج (HSSE) كـ "متوفر الآن" والباقي "قريباً"، وذلك بناءً على ترتيب المنتج في المصفوفة (`i === 0`).

## المطلوب
تفعيل 4 أنظمة: **السلامة (hsse)، القانونية (legal)، المهام (tasks)، الحجوزات (booking)**

## التغييرات

### 1. إضافة حقل `available` في `src/data/products.ts`
- إضافة `available?: boolean` للـ `Product` interface
- تعيين `available: true` للمنتجات الأربعة: `hsse`, `legal`, `tasks`, `booking`

### 2. تحديث `src/components/home/BentoGrid.tsx`
- تغيير منطق عرض الشارة من `i === 0` إلى `product.available`
- نفس الشيء لحجم البطاقة الأولى — المنتجات المتوفرة تظهر بالتنسيق المميز

### 3. ترتيب المنتجات
- نقل المنتجات الـ 4 المتوفرة للأعلى في المصفوفة لتظهر أولاً في الشبكة

